site_name: Knowledge Base

theme:
  name: material
  features:
    - navigation.instant
    - navigation.tabs
    - navigation.tabs.sticky
    - navigation.indexes
    - toc.integrate
    - navigation.top
    - search.suggest
    - search.highlight
  plugins:
    - search
  markdown_extensions:
    -attr_list
    -md_in_html
